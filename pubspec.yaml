name: modbus_codec
description: >-
  Transport-agnostic Modbus RTU codec for Dart. Encode request frames and
  decode raw response bytes into typed values over BLE, serial, or TCP.
version: 0.1.0
repository: https://github.com/anwin-kp/modbus_codec
issue_tracker: https://github.com/anwin-kp/modbus_codec/issues

topics:
  - modbus
  - rtu
  - protocol
  - serial
  - ble

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
